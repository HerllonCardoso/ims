{
  "name": "in-memory-filesystem",
  "version": "0.1.0",
  "private": true,
  "description": "In-memory filesystem with a CLI demo",
  "scripts": {
    "start": "ts-node src/cli/index.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
